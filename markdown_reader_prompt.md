# 轻量 Markdown 阅读器与记事本开发提示词 (Windows 桌面软件)

你将要开发一个 **Windows 桌面软件**，其定位为：**“轻量 Markdown 阅读器 + Markdown 记事本”**。

---

## 🎯 一、产品目标与核心定位
- **痛点解决**：专门解决 Windows 自带记事本打开 `.md` 文件阅读体验极差的问题。
- **产品体验**：软件要像系统记事本一样轻量、快速启动，但打开 `.md` 文件后**默认显示精美排版后的阅读界面**，营造出“翻开一本纸质书”的安静与人文感。
- **非复杂知识库**：不做双链、复杂关系图等重度功能，专注“轻、快、好读、能改”。

---

## 🛠️ 二、技术栈与环境约束 (必须严格遵守，不得混用版本)
- **核心框架**：**Tauri 2.x** (注意 2.x 与 1.x 权限、插件、命令机制完全不同，绝对不要引入 1.x 的 API)。
- **前端框架**：**React 18 + TypeScript 5**。
- **构建工具**：**Vite**。
- **包管理器**：**pnpm**。
- **状态管理**：**Zustand** (轻量化状态管理，严禁使用复杂的 Redux)。
- **本地持久化**：使用官方插件 **`tauri-plugin-store`** 将配置与历史写到应用本地数据目录，严禁使用浏览器 `localStorage` 作为主要存储。
- **渲染管线**：**react-markdown + remark-gfm + rehype-highlight**，辅以安全净化。

---

## 🎨 三、UI 视觉与设计规范 (暖色纸感风格)

### 1. 核心视觉气质
以**“温暖、克制、类纸张、人文感”**为核心气质，拒绝冷冰冰、工业感强的科技蓝紫风。通过柔和的暖色调、充裕的留白、微小的色差进行视觉分层。**全局禁用：纯黑纯白、粗黑边框、直角卡片、高饱和度色块、系统默认控件。**

### 2. 独家暖色纸感色板 (Tailored Color Palette)
所有中性色都必须偏暖调（带黄/褐感），严禁使用纯冷灰色。

*   **强调色 (陶土橘 - 灵魂所在)**：
    *   `--accent`: `#CC785C` (用于星标、选中状态、勾选框、当前目录项)
    *   `--accent-hover`: `#B8654B`
*   **浅色/护眼纸张主题 (默认推荐)**：
    *   `--bg-app`: `#F0EAD9` (应用背景底色)
    *   `--paper`: `#FBFAF7` (编辑区/搜索框等高亮面板色)
    *   `--panel`: `#F5F1E7` (侧边栏、工具栏底色)
    *   `--hover`: `#EAE4D5` (悬浮态背景色)
    *   `--text-primary`: `#2B2925` (暖近黑，非纯黑)
    *   `--text-secondary`: `#6B6659` (次要文字)
    *   `--text-muted`: `#9A9483` (禁用/辅助暗字)
    *   `--border`: `#E4DFD1` (柔和细分割线)
*   **深色/夜间纸张主题**：
    *   `--bg-app`: `#262420` (暖褐灰，非纯黑)
    *   `--paper`: `#2F2C27`
    *   `--text-primary`: `#EDEBE3`
    *   `--text-secondary`: `#A8A296`
    *   `--border`: `#3E3A33`
    *   `--accent`: `#D98E72` (深色强调色略微降噪)

### 3. 字体系统
- **界面 UI 与正文**：`"Inter", "Segoe UI", system-ui, -apple-system, sans-serif`，中文回退 `"PingFang SC", "Microsoft YaHei"`。
- **大标题 (H1/H2) 建议引入衬线体**：`"Georgia", "Songti SC", "SimSun", serif`，以增强纸质书质感。
- **代码/等宽字体**：`"JetBrains Mono", "Cascadia Code", "Consolas", monospace`。
- **字阶**：UI 辅助字 12px / 正常字 14px / 次标题 16px / 主标题 20px / 篇章大标题 26px - 30px。字重限定使用 `400`(常规) / `500`(中等) / `600`(粗体)。

### 4. 间距、圆角与阴影 (8px 栅格)
- **间距**：所有内边距、外边距、间隙必须符合 8px 栅格（只采用 4/8/12/16/24/32/48px），排版要保持高透气性。
- **圆角**：小元素(按钮、输入框、标签)使用 `8px`；主面板/卡片使用 `12px`；弹窗使用 `16px`。**全局禁止直角。**
- **阴影**：极轻、极暖、低透明度：`box-shadow: 0 1px 2px rgba(80,70,50,0.05), 0 6px 16px rgba(80,70,50,0.08);`。
- **滚动条**：自定义细窄滚动条 (6px–8px)，在 Hover 时显现，禁用 Windows 默认的宽灰滚动条。

---

## 📐 四、界面布局规划 (三栏式经典排版)

整个应用窗口在视觉上分为三个逻辑区域：

```
┌────────────────────────────────────────────────────────────────────────┐
│ [打开] [新建] [保存] [★]  产品需求文档.md       [ 阅读 | 编辑 ] [主题切换]  │  <-- 顶部工具栏 (48px)
├──────────────┬──────────────────────────────────────────┬──────────────┤
│ 🔍 搜索文件…  │                                          │ 目录         │
│              │ # 产品需求文档                             │             │
│ ⭐ 收藏       │                                          │  产品需求文档 │
│  └ 风格指南  │  这是一款轻量的 Markdown 阅读器...          │  核心功能    │
│              │                                          │  技术栈      │
│ 🕒 最近打开   │  > 设计原则: 轻、快、好读。               │  待办清单    │
│  ├ 需求文档  │                                          │              │
│  └ 会议纪要  │  ## 核心功能                              │              │
│              │  支持本地文件打开、历史持久化等...          │              │
│              │                                          │              │
└──────────────┴──────────────────────────────────────────┴──────────────┘
  左侧边栏 (250px)       主阅读/编辑区 (最大内容宽度: 760px–900px)      右侧目录栏 (210px)
```

1.  **顶部工具栏 (Header)**:
    *   高度 48px，背景为 `--panel`，底部一条 0.5px `--border` 细线。
    *   左侧放置核心动作图标（打开、新建、保存、分隔线、收藏状态星标、当前文件名带陶土橘圆点）。
    *   右侧放置“阅读/编辑”双态切换 Pill（滑块过渡，选中背景为陶土橘），以及主题切换按钮。
2.  **左侧固定侧边栏 (Sidebar)**:
    *   宽度固定 250px，背景为 `--panel`，右侧 0.5px `--border` 分割。
    *   顶部为温暖质感的搜索框（搜索历史记录）。
    *   分类展示“收藏文件”与“最近打开”。
    *   单项采用 `sel` 选中态：陶土橘低透明度背景 `rgba(204,120,92,0.12)` + 左侧 3px 陶土橘垂直指示条。
3.  **中部主干区域 (Main Area)**:
    *   自适应剩余宽度，背景采用温润的 `--bg-app` 纸色底。
    *   **阅读模式**：正文排版区域最大宽度限制在 `760px` 到 `900px` 之间，左右居中留白。正文字号 `16px–18px`，行高 `1.7` 左右，段落间距清晰。
    *   **编辑模式**：去除最大宽度限制，转换为轻量 Markdown 编辑器界面，并提供极佳的字体对比度和行号显示。
4.  **右侧导航目录栏 (TOC Sidebar)**:
    *   宽度固定 210px，背景与主干一致，左侧以 0.5px `--border` 隔离。
    *   实时抓取当前文档的 H1-H4 标题，点击可平滑滚动跳转。当前激活标题呈现陶土橘。

---

## ⚡ 五、核心功能逻辑与数据模型设计

### 1. 历史与收藏数据结构 (Zustand + tauri-plugin-store)
单条记录定义为：
```typescript
interface HistoryItem {
  path: string;                // 本地绝对路径
  title: string;               // 文件名 (去除 .md 后缀)
  lastOpenedAt: number;        // 时间戳
  isFavorite: boolean;         // 是否被收藏
  scrollPositionRatio: number; // 滚动百分比 (0 到 1 之间的浮点数)
}
```
*   **持久化机制**：当历史条目发生变更时，触发 `tauri-plugin-store` 异步落盘。
*   **淘汰机制**：历史记录**最多保存 50 条**，超过 50 条时，自动移除 `lastOpenedAt` 最旧的非收藏文件。
*   **失效处理**：若用户在外部删除了本地文件，在软件内点击历史时，应显示柔和优雅的提示弹窗，并提供“从历史中清除”按钮。

### 2. 滚动位置百分比记忆 (Scroll Memory)
*   **为什么用百分比**：避免使用像素（窗口拉伸后失效）或行号（编辑修改后失效）。
*   **实现细节**：监听阅读区域的滚动事件，使用节流（Throttle）技术每隔 500ms 将滚动高度百分比 `scrollTop / (scrollHeight - clientHeight)` 写入 Zustand。打开新文件时，等待渲染管线就绪，使用 `window.scrollTo` 恢复到对应的百分比。

### 3. Markdown 安全渲染与本地路径图片解析
*   **安全要求**：渲染器默认禁用 `rehype-raw`（或强制接入 `rehype-sanitize` 过滤掉不安全脚本），防止不可信文档执行远程恶意代码。
*   **外链拦截**：拦截所有外部超链接 `<a>` 标签点击事件，使用 Tauri 的 `shell` 插件通过**系统默认浏览器**在应用外部打开。
*   **本地图片解析路径（重点）**：
    由于 Tauri 存在沙箱限制，Markdown 中的本地相对路径图片（如 `![](./assets/image.png)`) 默认会裂开。**必须通过以下方式处理**：
    1.  获取当前打开 `.md` 文件的绝对目录路径。
    2.  将图片的相对路径拼接为绝对路径。
    3.  使用 Tauri 2.x 的 `convertFileSrc` 协议，把该本地文件绝对路径转换为安全的内置资源 URL 传入渲染管线。

---

## 📅 六、开发阶段边界与扩展预留

### 1. 第一阶段 (本次 MVP 必须完整交付)
- 本地文件一键打开、读取与保存。
- 暖色纸感界面排版（包含左边栏、顶部工具栏、目录、滚动条美化）。
- 历史记录持久化（最多50条、最近时间戳淘汰、失效文件清理）。
- 收藏/取消收藏功能、浅色/深色/纸色主题切换。
- 滚动百分比记忆与恢复。

### 2. 第二阶段预留 (本次仅预留架构接口，不写具体业务代码)
请在架构、前端组件及 Rust 端对应位置**预留好空接口和注释**：
- **Mermaid 图表** (预留 rehype/remark 扩展点)。
- **KaTeX 数学公式** (预留 rehype-katex 的注入接口)。
- **PDF/Word 导出** (预留前端 Print/PDF 触发函数与 Rust 端导出命令占位符)。
- **Windows 右键菜单绑定** (在 `tauri.conf.json` 中留出 `file_associations` 注册关联字段)。

---

## 🏛️ 七、项目架构与目录结构参考

```
md-reader/
├── src-tauri/                      # Rust 后端 (Tauri 2.x)
│   ├── src/
│   │   ├── main.rs                 # 程序入口，注册官方 Store 与自定义 Command
│   │   └── commands.rs             # 预留给后续重活 (如 PDF 导出) 的 Rust 指令位置
│   ├── capabilities/
│   │   └── default.json            # Tauri 2.x 权限清单 (核心！需配齐 fs, dialog, store, shell 权限)
│   ├── Cargo.toml
│   └── tauri.conf.json             # Tauri 主配置文件，预留文件后缀关联
│
├── src/                            # React 18 + TS 5 前端
│   ├── main.tsx
│   ├── App.tsx                     # 根容器，挂载 data-theme 主题
│   │
│   ├── components/                 # UI 组件
│   │   ├── Toolbar/                # 顶部工具栏
│   │   ├── Sidebar/                # 左侧边栏 (收藏、最近、搜索)
│   │   ├── Reader/                 # 主阅读区
│   │   ├── Editor/                 # 主源码编辑器
│   │   └── Toc/                    # 右侧目录导航
│   │
│   ├── features/                   # 核心业务逻辑
│   │   ├── markdown/
│   │   │   ├── MarkdownView.tsx    # 封装 react-markdown 渲染
│   │   │   ├── plugins.ts          # rehype-sanitize, code-highlight 配置
│   │   │   └── linkHandler.ts      # 外链拦截 + 本地相对路径图片解析
│   │   ├── toc/
│   │   │   └── useToc.ts           # 提取 H1-H4 目录
│   │   └── scroll/
│   │       └── useScrollMemory.ts  # 节流滚动百分比记录器
│   │
│   ├── store/                      # Zustand Stores
│   │   ├── useFileStore.ts         # 管理当前编辑文件与源码
│   │   ├── useHistoryStore.ts      # 管理持久化历史 & 收藏列表
│   │   └── useThemeStore.ts        # 管理暖色三套主题状态
│   │
│   ├── services/                   # Tauri API 外部隔离封装层 (唯一出口)
│   │   ├── fileService.ts          # 封装文件 open/save/dialog
│   │   ├── persistService.ts       # 封装本地 Store 同步
│   │   └── shellService.ts         # 封装系统外链打开
│   │
│   ├── themes/
│   │   ├── themes.css              # 三套主题 CSS 变量定义
│   │   └── markdown.css            # 正文排版样式规则
│   └── types/
│       └── index.ts                # 统一的 TypeScript 类型声明
```

---

## 🚀 八、输出与交付要求 (交付两步走)

### 💡 请分两步开始向我交付：

*   **第一步 (立即输出)**：
    请先展示该软件的**系统设计概要、Tauri 2.x 配置文件 (`tauri.conf.json` 与 `capabilities/default.json`)**。请向我说明你如何完美解决“本地相对路径图片在 Tauri 沙箱内解析”的问题。**请先不要开始吐出大坨前端 React 代码**，等待我确认设计无误后再继续。
*   **第二步 (当我回复“继续”后)**：
    届时请分模块输出完整、可贴入运行的项目代码（前端各核心组件、Zustand Store、样式文件），提供极简的依赖安装、运行开发服务器及打包 Windows 应用的方式。并在代码中附带详尽的**中文注释**。
