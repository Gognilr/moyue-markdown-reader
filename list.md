# Markdown 阅读器产品创新与实施清单

> 更新日期：2026-07-16  
> 产品北极星：成为第一个把 Markdown 从“排版后的长页面”变成“可追溯认知任务”的 Windows 本地阅读器，同时保持启动快、中文排版舒服和双击即读。  
> 核心表达：**翻开 `.md`，像翻开一本书；再次打开，不只是回到原位置，而是回到上次的思考。**

## 状态标记

- `[x]`：源码中已有实现或可运行原型。
- `[~]`：已有部分实现，但尚未达到可发布质量。
- `[ ]`：待设计或待实施。
- `[!]`：当前阻塞项，应优先处理。

## 最近修复记录

- [~] 2026-08-05 修复阅读模式搜索“下一个”不滚动：改为按渲染文本节点定位并滚动阅读容器，增加上一处/下一处与临时命中提示；编辑模式接入同一 Ctrl+F 搜索入口，支持搜索、上一个/下一个、替换和全部替换。新增搜索模型回归测试，TypeScript/生产构建通过，待新版安装包原生复验。
- [~] 2026-08-05 左侧最近文档与收藏条目新增按项“从磁盘刷新”按钮：刷新已打开页签会经过未保存保护并读取最新内容，未打开历史项会按最新磁盘版本重新打开；打开或切换文档会刷新 `lastOpenedAt`，最近项自动置顶，收藏状态保持不变。相关 TypeScript 构建与历史单测通过，待新版安装包原生复验。
- [~] 2026-07-28 修复左侧“最近文档”在启动后丢失：历史持久化与文件关联/拖入打开存在并行竞态，旧历史加载完成后会覆盖刚打开的新文档。现改为按路径合并持久化历史与运行时历史，新打开条目优先，仍保留收藏与最多 50 条最近记录；新增回归单测，8 项相关 Vitest 与生产构建通过，待新版安装包原生复验。

## 一、当前项目理解

### 1.1 产品定位

- [x] 本地优先，不依赖账号和云端服务。
- [x] 默认进入排版后的阅读模式，而不是源码编辑模式。
- [x] 定位为“轻量 Markdown 阅读器 + 能随手修改的记事本”。
- [x] 暖纸色、人文感、低干扰的视觉方向已经成立。
- [x] 明确不做复杂双链、知识图谱和重型知识库。
- [ ] 后续所有功能都要经过“是否破坏轻、快、好读”检查。

### 1.2 已有功能

- [x] Tauri 2 + React 18 + TypeScript + Vite + Zustand。
- [x] 打开、新建、编辑、保存 Markdown 文件。
- [x] 阅读/编辑双模式切换。
- [x] 纸张、浅色、深色三套主题。
- [x] 收藏和最近打开历史。
- [x] 阅读滚动百分比记忆。
- [x] H1-H4 目录提取和章节跳转。
- [x] GFM 表格、任务列表、代码高亮。
- [x] 外链交给系统浏览器打开。
- [x] Tauri 本地相对图片路径转换。
- [x] `.md`、`.markdown` 文件关联配置。
- [x] PDF 导出 Rust 占位命令。

### 1.3 当前阻塞与基础质量

- [x] `npm run build` 已于 2026-07-16 通过（TypeScript + Vite 生产构建）。
- [x] `src/store/useHistoryStore.ts` 已修正为 `../types` 导入。
- [x] Markdown remark/rehype 插件已补充 `PluggableList` 类型约束。
- [~] 已有 Vitest 单元测试覆盖历史记录淘汰与滚动位置；组件、端到端和导出回归样本待补齐。
- [~] Rust stable、`cargo check --release`、release 可执行文件与 NSIS 安装包已验证；MSI 和干净 Windows 安装验证待完成。
- [~] 已实现启动参数读取并在前端打开 `.md/.markdown`；仍待安装后双击实测。
- [~] 切换文件、新建文件、历史记录打开与 Tauri 关闭窗口前均已提供保护；Tauri 原生行为待实机验证。
- [~] 外部编辑器修改文件后，未修改文档可自动刷新；本地有修改时提供保留本地或采用磁盘版本。主阅读器现可将当前内容与磁盘版本或指定本地 Markdown 做只读渲染态比较；保存历史和 Git 版本待续。
- [x] 已移除浏览器原生 `alert/confirm`，错误与状态改为应用内非阻塞通知。
- [~] 已移除 capability 与 `assetProtocol` 的全局 `**` 文件范围：原生打开/另存为选择器及文件关联启动会按用户实际选择的 Markdown 所在目录动态授予 FS 与 asset scope；原生读取、相邻文档枚举和资源盘点另有授权状态校验。仍须在 Windows 安装包中实测打开、保存、监听与本地图片。
- [x] 已增加 README、开发/构建说明和发布检查清单。

## 二、GitHub 同类项目调研结论

### 2.1 可借鉴项目

- [MarkText](https://github.com/marktext/marktext)
  - 所见即所得、专注模式、打字机模式、数学公式、图片粘贴、PDF/HTML 导出。
  - 借鉴编辑手感和专注写作，不复制其完整编辑器复杂度。
- [Zettlr](https://github.com/Zettlr/Zettlr)
  - 全文搜索、引用管理、Pandoc 导出、自定义 CSS、出版工作流。
  - 借鉴搜索和模板导出，不走重型学术出版工作台路线。
- [Noteriv](https://github.com/thejacedev/Noteriv)
  - 同为 Tauri 2，具备文件监听、分栏、恢复、Mermaid、KaTeX、Git、插件和 AI/MCP。
  - 借鉴文件可靠性、恢复和分栏，不走 Vault、图谱、插件市场路线。
- [Moraya](https://github.com/zouwei/moraya)
  - Tauri 2、约 10MB、WYSIWYG、本地 AI 和 MCP。
  - 证明轻量与现代功能可以共存，但通用 AI 已经不是稀缺特色。
- [Glow](https://github.com/charmbracelet/glow)
  - 自动发现目录和 Git 仓库中的 Markdown，也能读取 URL 文档。
  - 借鉴无需建库的“项目文档漫游”。
- [Markdown Viewer](https://github.com/markdown-viewer/obsidian)
  - Mermaid、Graphviz、Vega、LaTeX、智能缓存、缩放、Word 导出。
  - 借鉴技术文档渲染、增量缓存和导出能力。
- [Readest](https://github.com/readest/readest)
  - 高亮、批注、双文档阅读、TTS、阅读标尺、段落专注、无障碍。
  - 借鉴真正属于阅读器的能力，而不是继续堆编辑器功能。
- [QuickLook](https://github.com/QL-Win/QuickLook)
  - Windows 资源管理器选中文件后按空格即时预览。
  - 借鉴 Windows 原生入口和极短操作路径。

### 2.2 明确不追逐的拥挤方向

- [ ] 不把知识图谱、双链、反向链接作为主线。
- [ ] 不在早期建设插件市场。
- [ ] 不强制用户建立 Vault 或导入资料库。
- [ ] 不以云同步和账号体系换取用户锁定。
- [ ] 不进行后台全盘 AI 索引。
- [ ] 不把聊天机器人放在产品中心位置。
- [ ] 不为了导出功能把 LibreOffice、完整 Chromium 等重型运行时直接塞进主安装包。

## 三、首先补齐的可靠阅读底座

### 3.1 文件可靠性

- [~] 文件变更监听已接入：无本地修改时自动刷新；本地有修改时保留内容并提示冲突。待 Rust 原生运行验证。
- [~] 视口锚定刷新已接入外部文件自动刷新路径：优先段落文本指纹、后备标题锚点；待原生长文档实测。
- [~] 冲突处理已支持保留本地和采用磁盘版本；主阅读器现可在本地比较当前、磁盘或指定文件的渲染态，审阅流程待续。
- [~] 自动恢复已保存未保存草稿并在下次启动时请求用户恢复，不会自动覆盖源文件；待原生运行验证。
- [~] 文件移动找回的保守匹配核心与确认 UI 已完成：历史项保存内容指纹与最多 8 个旧路径；文件不存在时用户可显式选择候选、查看指纹是否匹配并确认重绑，收藏和阅读位置会保留，绝不自动猜测。目录扫描候选、sidecar/外部工具移动后的完整同步仍待实现。
- [~] 桌面端编码读取已支持 UTF-8、UTF-8 BOM 与 GB18030 回退；待以真实遗留中文文件原生实测。
- [~] 原子保存已改为同目录临时文件写入成功后重命名替换；待 Rust 原生运行验证。

### 3.2 轻量交互

- [x] `Ctrl+O` 打开、`Ctrl+S` 保存、`Ctrl+F` 文内搜索、`Ctrl+P` 快速命令已接入。
- [~] Tauri 原生拖入 `.md` 文件已接入，待 Rust 原生运行验证。
- [x] 左右侧栏可独立折叠并本地记忆。
- [x] 窄窗口自动进入单栏阅读模式。
- [x] 已完成轻量多标签主界面：打开、切换、关闭、脏文档保存/放弃保护，以及每文档 100 步撤销/重做；不建设复杂工作区。
- [x] 未保存与草稿恢复使用自定义对话框，错误与状态使用非阻塞通知；文档级撤销/重做已接入编辑器工具栏与 `Ctrl+Z`、`Ctrl+Y`/`Ctrl+Shift+Z`，按活动文档隔离、禁用态明确，并始终由已保存检查点派生未保存状态。
- [x] 首次启动会直接说明“把 Markdown 拖进来，或双击 `.md`”，并提供选择文件入口；安装后双击关联仍需实测。

### 3.3 阅读器标配

- [x] 当前文档全文搜索和搜索结果计数。
- [x] 字号、行高、段宽、字间距和段间距均可即时调节并本地记忆。
- [x] 支持可调窄/宽段宽与窄窗口单栏；并提供紧凑、舒适、宽松三套本地阅读布局预设。
- [x] Mermaid（按需动态渲染）与 KaTeX 数学公式已支持；Front Matter、提示块和脚注的安全解析/可复用展示组件已接入主阅读页，保留 remark-gfm 原生脚注语义与反向链接，另提供可键盘访问的脚注导览、定义定位和返回首个引用位置。
- [x] 图片灯箱已支持点击放大、缩放/复位、复制图片地址与 `Esc` / 点击遮罩关闭；主阅读页已接入“打开原文件”：仅桌面端对资源清单已验证、且仍位于当前 Markdown 目录内的相对图片交由系统打开，浏览器预览明确提示无本地文件权限。
- [x] 代码块已支持复制、自动换行切换、语言标签和行号；复制始终保留原始代码文本。
- [~] 表格已支持横向滚动、冻结表头和第一列，以及不改原文的本地筛选、行列聚焦、TSV/Markdown 复制和数值列统计；全屏、排序、转置、单位格式化与长单元格展开待续。
- [~] 已提供跨平台快捷键与焦点循环基础，阅读区已使用 document landmark、F6 聚焦和搜索状态 live region；命令面板、未保存提示、草稿恢复、图片灯箱与表格均已接入模态焦点循环/恢复和状态播报，屏幕阅读器与视觉实测待续。

## 四、让用户喜欢并形成依赖的独特功能

### U1. “上次读到这里”恢复胶囊

  - [x] 关闭文档时记录最后标题、附近文本、停留时间和未处理任务。
  - [x] 再次打开时显示一张轻提示卡：上次读到哪个章节、停留多久、后面还有多少内容。
  - [x] 点击胶囊优先按附近文本回到语义段落，未匹配时才回退到持久化阅读比例。
- [~] 外部修改导致位置变化时，视口已优先用文本指纹、后备标题锚点重新定位；真实原生长文档仍待验证。

**价值：**用户会自然把它当成长期阅读技术文档、规范和长报告的固定入口。

### U2. 文档透镜

  - [x] “只看结论”：展示结论、总结、TL;DR、最终方案等语义段落。
  - [x] “只看行动”：汇集任务框、TODO、负责人、日期和待确认项。
  - [x] “只看风险”：汇集警告、注意、风险、限制、兼容性和失败条件。
- [x] “只看命令”：通过本地确定性规则汇集围栏代码块、Shell 命令、配置项和快捷键，并可跳回原文。
- [x] “只看数据”：通过本地确定性规则汇集 Markdown 表格、数字与指标、图表引用/定义和引用来源，并可跳回原文。
- [x] 第一版使用确定性规则本地解析，不依赖 AI。
- [ ] 可选本地 AI 只能增强分类，不替代原文，不上传全文。

**价值：**目录只回答“文档有哪些章节”，透镜直接回答“我现在最需要看的内容在哪里”。

### U3. 语义彩带导航

- [~] 在阅读区旁显示一条文档缩略彩带；标题、代码、图片、表格和任务已接入主阅读页，搜索/批注/警告/风险/未完成任务叠加已直接显示为可回跳标记，并复用增量计算；导出风险呈现待接入。
- [~] 标题、代码、图片、表格、任务使用克制的不同标记；警告、批注、搜索、风险与未完成任务均已接入主阅读区彩带，导出风险标记待接入。
- [x] 悬停显示内容摘要，点击直接跳转到对应渲染节点。
- [~] 搜索结果、未完成任务和风险叠加的扫描/增量 API 已完成，并已呈现于主阅读区彩带；导出风险待接入。

**价值：**长文档会变得像地图一样可感知，用户无需反复翻找。

### U4. 无侵入批注与稳定锚点

- [x] 已建立高亮、下划线、书签和边注的纯数据模型，默认不修改 Markdown 原文；主阅读渲染会在稳定锚点重定位成功后以可访问的高亮/下划线/书签/边注标记显示，批注可回跳原文；锚点失效时安全保留 sidecar 数据并在批注栏提示，不猜测位置、不改写 Markdown。
- [~] 已提供 localStorage sidecar 数据接口；桌面端已在打开时读取并在批注变更时写入同目录版本化 `.mdreader.json`，旧本机数据或 v1 sidecar 可由用户显式一键迁移；原生冲突提示待续。
- [x] 锚点由选中文本、前后文和所在标题共同构成。
- [x] 文档轻微修改后可按文本、邻域和标题路径重新寻找批注位置。
- [~] 可导出审阅版 Markdown 与 DOCX（原文加批注/摘录附录）；PDF 审阅导出待 P3 后端接入。
- [~] 已提供版本化 `.mdreader.json` sidecar、严格校验、旧 localStorage 数据迁移、桌面端原生读写和随身包文件模型；一键迁移 UI 待接入。

**价值：**这是“阅读器”相对于普通 Markdown 编辑器最有黏性的能力之一。

### U5. 摘录托盘

- [~] 已建立摘录数据模型与 Markdown 生成工具，保留标题路径、原文锚点和采集时间；阅读态托盘已接入类别筛选、排序、复制和键盘动作，拖拽与持久化手动排序待补齐。
- [~] 自动保留文档 key、标题路径、原文锚点和摘录时间；文件名展示由 UI 接入时补充。
- [~] 多条摘录可生成新的 Markdown 笔记；托盘已支持上移/下移与持久化手动排序，拖拽排序待续。
- [x] 支持“复制原文”“复制带来源引用”“返回原位置”：摘录托盘和“只看命令”均保留本地来源、可复制并可回到渲染原文。
- [x] localStorage sidecar 可持久保存；托盘支持查看、删除和导出审阅 Markdown。

**价值：**非常适合阅读需求、规范、会议纪要和研究材料时整理重点。

### U6. Live Mirror 外部编辑伴侣

- [~] 外部文件监听已让 VS Code、Cursor 或记事本保存后的阅读效果无闪烁刷新；语义彩带从同一份当前内容实时重算，仍待原生环境验证。
- [~] 监听刷新不重置阅读容器的滚动位置；标题级上下文锚定和变更提示待补齐。
- [ ] 外部保存后只增量重绘变化章节和图表。
- [~] 已提供桌面端明确动作，将当前阅读器窗口贴靠到当前屏幕工作区左/右半区（不遮挡任务栏）；可与“始终置顶”组合成为临时实时阅读镜。未安装环境、资源管理器 Snap Layout 与多显示器实机行为仍待 Windows 验证；浏览器预览不会假装控制系统窗口。
- [~] 已可从阅读工具面板显式打开只读的 Live Mirror 临时窗口：它携带当前内容快照及搜索/批注/健康叠加，关闭或刷新均不改动主阅读器；复用受限预览窗口并限制 4 MiB 快照。浏览器预览明确降级为当前页叠加面板，未伪装原生窗口；Windows 实机窗口行为仍待验证。

**价值：**不和专业编辑器竞争，而是成为任何编辑器都愿意搭配使用的阅读伴侣。

### U7. Table Lens 交互式表格阅读

<!-- 2026-07-16: 已完成本地全文筛选、行列聚焦、当前视图 TSV/Markdown 复制、数值列统计，以及只读派生视图中的稳定排序、转置、数字格式/手工单位后缀和长单元格展开；CSV、Excel 专项格式与自动单位识别仍待实现。 -->

- [x] 冻结表头和第一列。
- [x] 临时筛选、搜索、稳定排序均不修改 Markdown 原文；排序仅产生本地派生阅读视图。
- [x] 聚焦某一行或某一列，其余内容降低对比度。
- [x] 一键转置宽表，方便阅读“指标很多、记录很少”的表格；仅改变本地视图。
- [~] 数值列可切换千分位、百分比或紧凑显示，并可添加手工单位后缀；对表头中一致的显式单位提供保守建议，数值列使用等宽数字右对齐；复杂单位/列内对齐视觉验收待续。
- [x] 单元格内容过长时可在截断与展开之间切换，不把整张表撑坏。
- [~] 当前筛选结果可一键复制为 Markdown、TSV 或 RFC 4180 CSV（Excel 可直接导入）；已支持当前单元格/行/列复制，原生 `.xlsx` 导出与视觉验收待续。
- [x] 表格下方可显示数值列的合计、平均、最小、最大、空值数。

**价值：**多数 Markdown 软件只是“显示表格”，本产品可以让表格真正可读、可检查、可使用。

### U8. 渲染态文档对比

- [~] 已支持当前内容与磁盘版本、指定本地 Markdown、以及仅内存保存的上次成功保存版本的只读对比；外部版本来源待续。
- [~] 主阅读器已接入 `RenderDiffPanel`，按标题、段落、表格行、列表项和代码块显示语义差异；版本历史仍待接入。
- [~] 双栏同步滚动、差异点击回调、当前渲染节点定位及本地来源选择已接入；审阅工作流待续。
- [x] 表格按可配置主键列优先、同表最佳行相似度兜底匹配；移位行仍合并为单条修订，并在双栏面板突出单元格级增删改。
- [ ] 可导出带修订痕迹的审阅版 Word/PDF。

**价值：**需求文档、规范、方案和会议纪要的评审效率会明显提升。

### U9. 零配置项目文档漫游

- [x] 打开 README 后，自动识别同目录和子目录里的本地 Markdown 链接。（U9 最小闭环：从当前文档的相对 Markdown 链接即时推导，不扫描/建库）
- [x] 提供上一篇、下一篇、返回入口和面包屑。（已嵌入阅读视图；受控回调打开目标文档，目标不可访问时给出非阻塞提示）
- [x] 临时形成一本“项目手册”，无需创建 Vault 或导入资料库。（仅内存导航状态，无持久化索引）
- [~] 已完成受限真实目录扫描（当前文档父目录、Markdown-only、200 文件/2 MiB 上限）及失效链接/锚点、缺失图片、重复锚点和孤立文档诊断的主界面接入；跨目录索引待续。
- [~] 已提供 README/入口与引用关系驱动的可解释推荐阅读顺序，并已接入受限项目扫描后的主界面；跨目录索引待续。

**价值：**特别适合阅读 GitHub 项目、产品文档和离线技术手册。

### U10. 中文技术文档排版引擎

  - [x] 中英文、数字与标点的视觉间距优化。
  - [x] 避头尾、孤行控制、标题与下一段保持、中文标点挤压。
  - [x] 中文正文、英文、代码和数字分别选择最适合的字体回退链。
- [x] README、报告、论文、书籍、会议纪要五种排版人格，并可从工具栏的“阅读版式与排版人格”入口即时应用。
- [x] 支持仿宋公文、宋体书卷、现代黑体和技术文档主题；仅选择系统已有字体并提供安全回退链。
- [x] 可选竖排阅读，用于诗歌、古文和特殊内容展示：用户在阅读器中主动开关，默认关闭且仅改变屏幕排版；代码块和表格保留横向阅读，并提供屏幕阅读器状态说明。

**价值：**形成一眼就能认出的产品视觉，而不只是增加普通皮肤数量。

### U11. 自动排版人格

  - [~] 已按 README、技术文档、会议纪要、长文、报告和 API 手册进行本地规则识别；目录、代码和表格模式建议待补。
  - [~] 已推荐段宽、行距与字距预设；目录、代码和表格模式建议待补。
  - [x] 只做可撤销的展示建议，不改变文件内容。
- [x] 用户可固定某个文档或某类文档的排版人格；文档固定优先于类型固定，仅存于本地偏好且不修改 Markdown 源文件。

**价值：**用户打开文件就觉得“它知道这份文档应该怎样读”。

### U12. 阅读专注套件

- [x] 阅读标尺：突出当前一段。
- [x] 段落步进：空格键逐段推进，适合精读和演讲准备。
- [x] 自动滚动：速度可调，移动鼠标立即暂停。
  - [x] 本地 TTS 与专注计时器已接入主阅读页：Web Speech 仅在用户显式操作时朗读，不支持时清晰降级；代码、链接和表格可跳过或仅概述表头，计时状态仅本地持久化。
  - [x] TTS 可选择跳过代码、链接和表格，或概述表格的行列与表头。
  - [x] 专注计时仅保存在本地，不做成压力化的打卡系统。

### U13. 文档健康检查

- [~] 已完成可注入资源库存的文档健康检查，并提供仅限当前文档父目录相对资源的原生受限清单命令；主界面可导出不含正文和绝对路径的 JSON 健康报告；原生实机与完整导出链路验证待续。

### U14. Markdown 随身包

- [~] 已完成开放 ZIP 最小闭环：标准 Store ZIP（含 CRC32）内含 `manifest.json`、普通 Markdown/HTML、显式传入的相对资源、可选 CSS 主题与批注；严格拒绝绝对路径和目录穿越，工具栏可下载当前文档的基础包。工具栏尚未收集当前文档的资源、主题和批注，完整素材包需由导出 API 调用方显式传入。
- [~] 可从工具栏导入本应用的 `.mdpack.zip` 并以只读内存预览方式打开包内 Markdown；manifest、Store ZIP、CRC32、路径、资源声明均严格校验，显式声明的相对资源改写为会话 Blob URL，绝不解包或写入外部文件。浏览器用 file input，桌面端用原生选择器且不授予包的文件系统 scope；尚未实现包文件双击关联，也尚未让工具栏自动收集完整素材。
- [~] ZIP 可随时用任意标准工具解包为普通文件，不形成专有内容锁定；应用内已支持本应用 v1 Store ZIP 的受限导入/解包预览，不接受普通 ZIP 或压缩/多盘 ZIP。
  - [x] 可生成一个自包含 HTML 作为无需安装应用的分享版本，并已接入主工具栏下载。

### U15. Windows 即时阅读

- [ ] 双击 `.md` 后直接进入阅读态，不先显示空窗口。
- [x] 资源管理器可通过可选右键命令快速弹出无边框预览；入口可安装、可移除，卸载程序也会清理注册表，不注册全局快捷键。
- [x] 快速预览支持 `Esc` 关闭、`Enter` 转为完整窗口及左右方向键浏览同目录 Markdown；安装版已实测正文、中文、宽表和本地图片，且不加载主窗口阅读工具与工作台。
- [x] Windows Jump List 原生展示最近文档和收藏文档，过滤失效/非 Markdown 路径、去重且每类限制 10 项；安装版 COM 提交已通过。
- [~] 已支持始终置顶、明确动作贴靠当前屏幕工作区左/右半区和临时快速预览窗口；资源管理器 Snap Layout、多显示器与安装后原生行为待验证。

### U16. 克制的 AI：只问当前所见

- [x] AI 必须是可选功能，默认关闭，不后台索引文件系统。
- [x] 默认只准备用户明确选中的文字，扩大上下文前需要确认；当前不会发送网络请求。
- [~] 已提供 Ollama loopback 与用户 HTTPS/loopback 服务的严格配置/请求边界；实际模型调用 UI 待显式接入。
- [ ] 回答必须附带可点击的原文锚点。
- [ ] 重点场景是解释术语、生成当前章节摘要、比较两个段落、把表格转成结论。
- [ ] 不建设通用聊天中心，不让 AI 遮挡正文。

## 五、高保真 Word/PDF 导出专项

### 5.1 结论

- [ ] **可以显著解决同类软件的导出痛点，并把它做成核心卖点。**
- [ ] 不承诺“任意 Markdown、任意字体、任意 Word 版本都像素级完全相同”。Markdown 本身没有记录纸张、列宽、分页和合并单元格等全部排版信息。
- [ ] 对产品明确支持的语法和表格模型，可以实现：内容不丢失、结构正确、表格可编辑、分页可预测、PDF 视觉稳定、导出前可预览和诊断。
- [ ] 产品承诺使用“Office-ready 高保真”而不是无法验证的“绝对完美”。

### 5.2 为什么常见转换器会失败

- [ ] GFM 表格通常只有单元格内容和左中右对齐，没有真实列宽、纸张方向、重复表头和分页规则。
- [ ] HTML/CSS 表格布局与 WordprocessingML 表格布局不是同一套算法。
- [ ] Word 会受到字体、Office 版本、默认打印机度量和自动适应规则影响而重新分页。
- [ ] 宽表直接打印容易溢出、缩得过小，或把某些列挤成一字一行。
- [ ] 长表容易丢失重复表头、拆开同一行，或在页底留下难看的空白。
- [ ] 合并单元格、单元格内多段落、列表、代码和图片需要专门建模，不能靠字符串替换。

### 5.3 推荐架构：一份文档模型，两个专业后端

```text
Markdown / GFM / HTML table
            ↓
       Markdown AST
            ↓
标准化 Document IR（段落、样式、图片、表格、分页意图）
            ↓
      Layout Planner
   ┌────────┴────────┐
   ↓                 ↓
DOCX OOXML 后端    PDF Typst 后端
   ↓                 ↓
可编辑 Word         稳定分页 PDF
```

- [x] 建立与渲染 UI 解耦的 `DocumentIR`，不能直接把 React DOM 当导出输入。
- [~] 所有标题、段落、列表、代码、图片、公式、图表和表格先进入统一模型（已覆盖 GFM/数学/Mermaid 语义；复杂 HTML 后续需诊断）。
- [~] Word 与 PDF 共享语义、主题 token、列宽方案和分页意图（IR 与列宽规划器已建立；六类模板现已接入浏览器 DOCX/PDF 的字体、页边距、排版和表格颜色。页眉页脚、封面和横向分页意图仍待后端闭环）。
- [ ] 后端允许按格式做必要差异，不追求错误的逐像素强行统一。
- [ ] [Pandoc](https://pandoc.org/MANUAL.html) + `reference.docx` 可作为早期兼容后端、模板基准和回退方案，但不作为最终唯一的高保真表格引擎。

### 5.4 Smart Table Studio 表格排版工作台

- [x] 导出前自动扫描每张表，计算最小内容宽度、理想宽度和可压缩范围。
- [x] 自动识别文本、数字、百分比、金额、日期、代码和状态列。
- [~] 数值列右对齐，小数点视觉对齐，状态列居中，说明列优先获得宽度（默认对齐与说明列宽倾向已规划；后端视觉小数点对齐待实现）。
- [x] 提供“自动、均分、按内容、固定比例”四种列宽策略。
- [~] 已完成列宽手动覆盖与 sidecar 导出设置核心，并在“阅读工具 → 表格工作台”提供按表选择、单列宽度应用、策略选择与独立 sidecar 保存；DOCX/PDF 已消费列宽与预设偏好，拖拽调宽待续。
- [~] 宽表的横向页面、窄边距、缩小字号和关联拆表已在“表格工作台”提供选择/适配预览并保存为导出偏好；实际 DOCX/PDF 后端映射待续。
  - 横向页面。
  - 更窄页边距。
  - 合理缩小字号。
  - 分组拆成两张关联表。
  - 转为附录大表。
- [~] DOCX 与浏览器端 PDF 已实现长表重复表头；DOCX 现可实际生成模板封面、页眉与页脚页码；Typst/Word 同版 PDF 后端仍待接入。
- [~] DOCX 已映射普通行 `cantSplit`，PDF 普通行保持整行换页；超高单行会以重复表头的 continuation 分片输出并记录明确降级说明。Office 实际分页视觉验证仍待完成。
- [~] 对显式 `表/Table` 标题、`注/备注`、`来源/Source` 标记，统一 IR 会将其与表格绑定并由 DOCX/PDF 在表前/后邻近输出；自动编号、实际 Word/PDF 视觉位置仍待 Office/查看器验证。
- [~] DOCX/PDF 已按共享模板 token 支持条纹行、首列强调、识别到的总计/合计行与可选单色打印模式；条件色、深色阅读转浅色打印和视觉回归待续。
- [ ] 支持合并单元格扩展：HTML table、GUI 设置或 sidecar 描述。
- [~] 已完成内容/结构/位置指纹重关联，重复结构拒绝猜测；工作台加载 sidecar 时已接入重关联，主 DOCX/PDF 导出流程消费偏好待续。

### 5.5 Word DOCX 后端

- [x] 直接生成 WordprocessingML/OOXML，而不是将 HTML 粗暴转换成 Word。
- [x] 使用成熟的 [`docx`](https://github.com/dolanmiu/docx) TypeScript 库作为浏览器端起点。
- [x] 生成明确的 `tblGrid`、表格总宽度、每列宽度和固定布局策略。
- [x] 使用 Word 原生重复表头属性。
- [x] 设置行不可拆分、单元格边距、垂直对齐、底纹和段落样式。
- [ ] 支持横向 section，让单张宽表进入横向页，后续正文恢复纵向。
- [ ] 使用 `reference.docx` 作为用户可定制模板来源：页面、页眉、页脚、字体和样式。
- [x] 表格在 Word 中保持真实可编辑结构，不能栅格化成图片。
- [~] 已有 IR→DOCX 结构计划与 OOXML 二进制关键属性回归（`tblHeader`、`cantSplit`）；六类模板已实际映射字体、字号、页边距、代码/表头颜色，图片关系、横向 section、页眉页脚/封面和 Office 实际视觉验证待补齐。

### 5.6 PDF 后端

- [ ] 优先评估将 [Typst](https://typst.app/docs/reference/pdf/) 作为 Rust 导出引擎或可选导出组件。
- [ ] Typst 原生支持分页文档和跨页表格，并能重复表头/表尾。
- [ ] 统一处理页眉页脚、页码、目录、交叉引用、横向页面和字体嵌入。
- [~] 已提供浏览器端独立 PDF Blob 后端：基于 `DocumentIR` 真实绘制矢量文本/表格、A4/Letter 分页、普通行整行换页与表头跨页重复；Markdown 外链会生成 PDF URI 链接注释，并写入基础可访问性主题元数据；Unicode/CJK 字体嵌入、代码语义复制和 PDF/A/无障碍仍待补齐。
- [ ] 提供 PDF/A 和可访问 PDF 选项时，必须用真实规范检查而不是只改文件后缀。
- [ ] HTML + [Paged.js](https://pagedjs.org/) 可作为主题快速原型或兼容后端，不作为唯一高保真引擎。

### 5.7 两种 PDF 模式

- [ ] **独立 PDF**：由 Typst 直接排版，不要求安装 Microsoft Word，分页质量由本应用控制。
- [ ] **Word 同版 PDF**：当 Windows 已安装 Word 时，先生成 DOCX，再调用 Word 的 `ExportAsFixedFormat` 输出 PDF。
- [ ] “Word 同版 PDF”能最大限度保证 PDF 与用户打开的 Word 版本分页一致。
- [ ] 未安装 Word 时自动隐藏或禁用同版模式，并清楚说明独立 PDF 的差异。
- [ ] 自动化过程使用临时副本、只读打开、超时和进程清理，避免影响用户正在编辑的 Word 文档。

### 5.8 导出预检与自愈

- [ ] 导出前显示页面级预览，而不是导出后让用户反复试错。
- [~] 已建立并接入 DocumentIR 导出前预检：识别附件清单缺失/不安全资源、远程资源、超宽表和原始 Markdown HTML，并给出修复建议与阻塞；字体、损坏图片、孤行和后端实测待原生导出接入。
- [~] 预检面板已显示“需要选择、无法保证”处置状态；处置状态独立于 A/B/C。预留的自动修复状态尚无真实后端修复实现，不能据此声称 HTML 已修复；缺失/不安全资源仍阻止导出。
- [~] 远程资源可选择保留或仅从导出副本省略，且不改写 Markdown；自动修复仅为预留机制，待有可证明的转换实现后再启用。
- [~] 导出给出明确的可信度等级、结构性证据和降级原因（工具栏、预检面板和导出结果均展示）：
  - A：DocumentIR 与确定性预检规则通过；**不代表** Word/PDF 阅读器、字体替换或像素级布局已验证。
  - B：可导出，但存在已记录的兼容性或降级风险（例如远程资源、超宽表或不受契约支持的 HTML）。
  - C：存在未解决的确定性阻塞项，不能导出。
  - 字体可用性、真实版式与视觉验证仍是待完成的独立门禁，不能被 A/B/C 替代。
- [ ] 绝不静默截断表格、图片或公式。

### 5.9 导出模板

- [x] 内置技术报告、需求文档、会议纪要、学术简稿、中文公文、README 六类模板。
- [x] 模板统一定义颜色、字体、页边距、标题、代码、表格和页眉页脚 token。
- [~] 工具栏可选择六类模板用于 DOCX/PDF；预检会展示所选页面、字号、行高和主题色 token，导出只读取模板，不改写 Markdown。页眉页脚、封面、Logo 和全部 token 的格式后端映射仍待补齐。
- [ ] 用户可导入自己的 `reference.docx`。
- [x] 用户可保存表格排版预设并按文档类型自动选择（预设库保存在本地；文档 sidecar 仅保存显式选择；文档级例外优先，均不改 Markdown）。
- [~] DOCX 已支持封面、页眉/页脚页码，并读取 front matter 的密级、版本、作者和日期；公司 Logo 待实现。

### 5.10 高保真验收矩阵

- [~] 已建立中英混排、公式、代码、资源与 50×8 长表的 Markdown 黄金样本和结构回归：验证共享 IR、DOCX 真实表格/表头/`cantSplit`/文本，以及 PDF 文件头/分页/重复表头；另有超高单行 continuation 回归和预检风险验证。Office/截图/PDF CJK 视觉回归待补齐。
- [ ] 表格列数：2、4、8、12 列。
- [ ] 表格长度：1、50、500 行。
- [ ] 内容：纯中文、中英混排、长 URL、长代码、金额、百分比、日期、Emoji。
- [ ] 单元格：多段落、列表、代码、图片、换行、空值。
- [ ] 结构：重复表头、汇总行、横向页面、合并单元格。
- [ ] 分页：表格从页中开始、跨 2 页、跨多页、单行接近一页高度。
- [ ] 主题：彩色、黑白打印、深色阅读转浅色打印。
- [ ] 应用：Microsoft Word 2021、Microsoft 365 当前版、LibreOffice 当前稳定版。
- [ ] PDF：Chrome、Edge、Adobe Acrobat、系统预览器。
- [ ] 每个样本保留黄金 DOCX、PDF 和页面截图，防止后续升级导致版式回退。

### 5.11 轻量化策略

- [ ] 阅读、编辑和普通渲染不加载导出引擎。
- [ ] 高级导出引擎可以作为按需安装的“导出增强包”。
- [ ] 不捆绑完整 LibreOffice。
- [ ] 图表和公式只缓存转换结果，不重复渲染未变化节点。
- [ ] 大文档导出运行在后台任务中，支持取消、进度、错误详情和临时文件清理。

## 六、实施顺序

### P0：可构建、可信任

- [~] 2026-07-20 品牌资产最终选择：产品正式名称保持“墨阅Markdown阅读器”；用户通过截图明确指定“深色底 + 奶白 M + 红色书签”的简洁版为正式系统图标。此前书页墨迹版及像素对齐书页实验均不进入当前安装包；Tauri/EXE、NSIS 安装/卸载器和桌面快捷方式分别使用同版 `icon-v2.ico`、`installer-v2.ico`、`shortcut-v2.ico`。生产/NSIS 构建通过，直接从 EXE 与安装包提取的 32px 图标哈希一致并确认为指定版本；待覆盖安装实机复验，旧任务栏固定项须取消固定后重新固定。
- [~] 2026-07-20 修复竖排阅读没有可见滚动条：竖排正文改为独立、定高的横向滚动视口，长文从右向左溢出时强制显示 10px 强调色滚动条，并限制横向过度滚动不影响应用外层；新增 Edge E2E 校验 `vertical-rl`、`overflow-x: scroll` 及实际 `scrollWidth > clientWidth`。220 项 Vitest、5 条 Edge E2E、20 项 Rust 测试与生产/NSIS 构建通过，待安装包视觉复验。
- [~] 2026-07-19 修复历史文件“未移动却提示可能移动”：原逻辑把文件系统权限拒绝与文件不存在统一折叠为 `false`。现由 Rust 只读检查明确区分“已授权可打开 / 文件存在但本次启动需重新授权 / 确实不存在 / 历史路径无效”；`I:\E:\...` 这类双盘符损坏记录不再误报移动，重新选择同一原文件会直接授权并打开，只有选择不同文件时才要求确认重绑。前端 200 项、Rust 15 项测试与生产构建通过，待新安装包实机复验。
- [~] 2026-07-20 继续修复“关闭软件后点击最近文件仍要求重新授权”：根因是 Tauri 动态 FS/asset scope 仅在当前进程有效。原生层现持久化用户明确选择过的精确 Markdown 路径，并从旧版应用自有 `settings.json` 历史做兼容迁移；重启后点击最近文件只会恢复已记录文档的目录授权，未知路径仍必须经过系统选择器。220 项 Vitest、4 条 Edge E2E、20 项 Rust 测试与生产/NSIS 构建通过，待安装包重启实机复验。
- [~] 2026-07-19 修复继续阅读、阅读工具与批注面板回归：恢复位置只展开 Markdown 正文内的 `<details>`，不再自动展开本机朗读设置；阅读工具改为视口顶部下方的固定面板，从长文任意位置均可打开；“批注与摘录”入口移入顶部控制栏，内容与批注工作台合并为一个顶部面板，删除正文底部悬浮横条。工具栏模板选择明确标为“导出 · 类型”，切换后即时提示仅对下次 Word/PDF 导出生效。待新安装包实机复验。
- [~] 2026-07-19 再次排查右上角 X 无法退出：已确认关闭拦截与确认弹窗能进入，但确认后的原生 `destroy()` 未包含在 Tauri 默认窗口权限中，导致最终销毁被 ACL 拒绝。现为主窗口能力显式增加 `core:window:allow-destroy`，并把关闭监听注册提前到拖放监听之前；无修改路径已用新 release EXE 发送原生窗口关闭消息验证，8 秒内正常退出且退出码为 0；有修改时仅在“保存并退出”或“放弃并退出”后执行一次原生销毁，仍待安装包实机点击复验。
- [~] 2026-07-19 修复目录遮挡与“下一段”破坏页面滚动：目录高亮和跳转不再使用固定 120px/`scrollIntoView()`，而是按阅读滚动容器及精简工具栏的实时高度计算目标位置；逐段推进与标尺只扫描正文中可见、非嵌套的阅读块，并只修改正文容器 `scrollTop`，末段不再强制滚到底或连带滚动应用祖先。阅读工具栏改为固定高度的精简入口栏，“专注 / 版式 / 阅读工具 / 批注与摘录”内容按需弹出，不采用会造成正文跳动和导航偏移漂移的自动隐藏。新增容器滚动边界单测；前端 202 项测试、生产构建与 NSIS 打包通过，待新安装包实机复验长文目录与连续逐段推进。
- [~] 2026-07-19 产品收敛：用户确认“阅读工具”实验工作台与“批注与摘录”偏离轻量阅读器初衷且完成度不足，现从阅读界面软弃用两个入口、对应面板及选中文本批注浮层；保留“专注”和“版式”作为轻量控制。已有本机批注、摘录与 `.mdreader.json` sidecar 数据不删除、不迁移，底层实验代码隔离保留，待核心打开/阅读/保存/导航稳定后再决定是否物理删除。
- [~] 2026-07-19 轻量工具栏继续收敛：“专注”和“版式”的控制内容改为在同一顶部栏内、入口按钮左侧就地展开，再次点击收起，不再显示下方独立浮层；窄窗口保持单行并允许控制区横向滚动，避免工具栏换行增加遮挡高度。
- [x] 2026-07-19 修复首次启动错误显示“建议：舒适阅读”：空白启动状态不再把默认推荐冒充为文档分析结果，只显示打开 Markdown 的首次引导；打开文件、新建含内容文档或恢复草稿后才允许显示排版建议。新增空白/已打开/新建文档显示条件回归测试；`204` 项测试、生产构建与 NSIS 打包均通过。
- [~] 2026-07-19 修复历史记录再次误报“路径无效”：Windows 合法扩展路径 `\\?\E:\...` 不再被双盘符损坏路径校验误伤，同时继续拒绝 `I:\E:\...` 这类真实损坏记录；Rust 新增扩展路径回归，16 项测试通过，新 NSIS 已生成，待安装包实机复验。
- [~] 2026-07-19 主工具栏增加“打印当前文档”按钮：无文档时禁用，编辑态打印会先切换到隔离的阅读正文，打印结束后恢复原模式；继续复用只打印 `print-document-content` 的打印样式。前端 204 项测试与生产构建通过，新 NSIS 已生成，待系统打印预览复验。

### 2026-07-19 核心收敛后的真实剩余工作

正式执行与验收安排见 [`核心完善执行计划-2026-07-19.md`](核心完善执行计划-2026-07-19.md)。五个阶段均已列入计划，当前从 P0-1 大文件性能开始执行；状态只有在各自代码、自动化及原生/视觉门槛全部通过后才会改为完成。

- [x] P0-1 大文件性能：2.74 MB / 4500 节基准已完成分段渲染、目录窗口化、隐藏工作台停算、导出引擎按需加载和表格降载。最终 EXE 主渲染进程私有内存约 173.9 MB，相对原始约 2703.4 MB 下降约 93.6%；12 次连续 PageDown 约 261 ms，目录同步且窗口持续响应。
- [x] P0-2 Windows 原生稳定性：命令行/安装版关联打开、无修改切换、页签正文同步、首次/连续覆盖保存、关闭保护取消/放弃、外部自动刷新及脏文档冲突选择均已通过；拖入由 Edge E2E 覆盖，权限边界由 Rust 测试覆盖。最终 NSIS 已完成覆盖升级、卸载清理和重装，关联命令恢复正确。
- [x] P1-1 DOCX/PDF 质量：本地图片已真实嵌入，PDF 使用 Windows CJK 字体子集并可复制/搜索中文；12 列 × 50 行黄金样本在 DOCX/PDF 中横向分页、重复表头且保持完整。DOCX 经 Word 渲染 14 页、独立 PDF 渲染 6 页，全部 PNG 逐页检查无缺字、裁切、重叠或空白断页；专项测试 21 项与最终 Rust 19 项通过。LibreOffice 本机未安装，未标记其兼容性完成。
- [x] P1-2 核心操作端到端回归：新增 Playwright/Edge 两条完整场景，覆盖首次启动、选择/拖入打开、阅读编辑切换、新建、多页签未保存保护、保存、打印及 XLSX/DOCX/PDF 导出，2/2 通过；统一 `npm run test:core` 串联 Vitest、Playwright 与 Rust。编写回归时修复“关闭非当前脏页签却保存当前页签”缺陷并补服务测试；Windows 原生打开/保存/关闭/外部修改保留独立实测证据。
- [x] P2 低优先级增强：XLSX 已由本机 Microsoft Excel 打开并读取正确单元格；Jump List 原生 COM 提交成功；资源管理器右键快速预览完成安装、触发、轻量渲染、卸载清理与默认关闭实测。
- [ ] 非本轮五阶段阻塞项：`npm audit --omit=dev` 仍有 Mermaid/KaTeX 依赖链 `1 high / 8 moderate`；自动修复会升级解析器并大幅重排依赖，需单独建立 Mermaid/公式视觉回归后处理。

以下盘点不包含已软弃用的“阅读工具”实验工作台、“批注与摘录”及其认知航线、理解账本、阅读任务、Live Mirror 等扩展实验，也不把“不做什么”的产品边界当成功能欠账。

1. **P0 基础可靠性与原生验收**：继续实测历史记录、文件关联、打开/拖入/切换/新建/保存/另存为、未保存保护、关闭、外部修改监听、本地图片和打印；完成干净 Windows 安装、升级、卸载与关联恢复验证。
2. **P0 大文件性能与内存**：停止为已隐藏实验功能预先计算多份 Document IR/健康报告/项目透镜数据；让 Mermaid、KaTeX、DOCX/PDF 等重模块按需加载；补真实大文件冷启动、打开、滚动、切换和峰值内存基线。
3. **P1 自动化保障**：补核心文件会话、打印和导出的组件/端到端回归；补真实 GB18030、长文档、本地图片及多文件切换样本。
4. **P1 导出闭环**：DOCX/PDF 仍需补图片/公式/CJK 字体、横向宽表、分页预览、Office/PDF 查看器视觉回归及大文件后台进度/取消；不把 Typst、Word 同版 PDF、`reference.docx` 定制视为基础版阻塞项。
5. **P2 表格与桌面增强**：视需求再做原生 XLSX、合并单元格、拖拽列宽；资源管理器快捷预览、Jump List 属于可选桌面增强，不阻塞轻量阅读核心。
6. **低优先级格式包**：自包含 HTML 仅用于离线分享，开放 ZIP 仅用于开放迁移；底层能力可保留，但不应长期占主工具栏。ZIP 自动收齐图片/主题等资源尚未闭环，建议以后统一收进“更多导出”。

- [~] 2026-07-19 基础文档会话回归已完成代码修复：磁盘读取现在一次性提交相同的非空快照到页签和正文 store，切换、拖拽、文件关联、历史重绑、快速预览、草稿恢复、外部刷新和新建文档不再通过会自动制造 dirty 状态的分步写入；已增加“打开后正文/页签一致且未修改”的回归测试。新 release EXE 已实机通过文件关联参数打开 14,773 字节 Markdown，正文与目录均正常；最终源码 `npm test` 197 项通过，切换、新建及未保存弹窗仍待最终安装包复验。
- [~] 2026-07-19 Windows 保存与 sidecar 回归已完成代码修复：原子保存改用 `MoveFileExW(REPLACE_EXISTING | WRITE_THROUGH)` 覆盖已有文件，`.mdreader.json` 与 `.mdreader.tables.json` 共用同一严格读写白名单；Rust 覆盖替换与白名单测试通过，待新安装包复验连续保存及“保存并继续”。
- [~] 2026-07-19 “继续阅读”已在定位前展开正文内全部 `<details>`，并有展开回归测试；打印改为结构化隔离应用工具栏、页签、侧栏、目录和阅读工具，只保留 `print-document-content`。待新安装包检查长文完整展开和系统打印预览。
- [~] 2026-07-20 修复启动空白页错误出现“继续阅读”并在点击后只显示“空空如也”：根因是恢复位置胶囊不含正文，却会为无路径或空正文的启动文档保存、加载检查点。现保存和加载两端都只允许“已有真实路径且正文非空”的文档使用位置恢复；新增空路径、未保存正文、空文件及真实文件四种回归，221 项 Vitest 通过，待安装包重启复验。
- [~] 2026-07-20 资源基线实测：release 版打开 105,574-byte、2,499 行、70 个围栏代码块的 Markdown，预热 30 秒后 Tauri + 6 个 WebView2 子进程合计工作集约 611.7MB、私有内存约 351.2MB；5 秒稳定采样约占单核 24.06% / 16 线程整机 1.50%。对轻量阅读器仍偏高，主要消耗位于 WebView2 渲染/GPU进程；此项仅完成基线和疑点定位，尚未达到大文件内存/CPU性能验收。
- [x] 修复 TypeScript 构建错误。
- [~] Rust 工具链已补齐，`cargo check --release`、release 可执行文件与 NSIS 安装包已验证；MSI 与干净 Windows 安装待验证。
- [~] 未保存修改保护（已覆盖文档替换与 Tauri 关闭窗口）、Windows 覆盖式原子保存和崩溃恢复已实现；2026-07-19 已将 Tauri 原生关闭请求从浏览器 `beforeunload` 中拆开，关闭有未保存内容时显示专用退出确认，多个脏页签不再允许“保存当前后丢失其他页签”。自动化回归通过，待新安装包原生验证。
- [~] 2026-07-19 阅读专注、版式控制与“阅读工具”已集中到正文上方的固定控制栏，避免遮挡或挤占正文中部；待新安装包目视验收窄窗口换行布局。
- [~] 2026-07-19 DOCX/PDF 导出改为原生“另存为”选取单个目标文件，再通过一次性授权写入；工具栏会显示预检/生成/保存阶段，结束后回显精确保存位置与可信度。自动化与 Rust 命令测试通过，待新安装包实测系统保存对话框及成品打开。
- [~] 2026-07-19 阅读排版建议“应用”现会立即写入布局预设、隐藏提示并按文档记住；窗口关闭的“保存并退出”改为依次保存所有脏页签，任一另存为取消或保存失败都会保留窗口。新增服务回归测试，待新安装包原生验收。
- [~] 2026-07-19 首次打开文件时正文空白、点击目录后才显示的问题已定位为旧滚动位置在新正文完成布局前恢复；改为等待新正文渲染后恢复，并在视口没有正文节点时回到文首。待新安装包实测。
- [~] 2026-07-20 再次修复每次启动错误提示“发现未保存草稿”及按钮无响应：实机 `settings.json` 证明确有残留 `recoveryDraft`；原实现先等待 Store 删除，删除卡住/失败会让弹窗永不关闭。现改为按钮先响应、后台清理，原生 Store 用 `null` tombstone 替代 `delete` 并设 1.5 秒超时；恢复内容通过专用 `restoreDraft` 保持 dirty 和编辑模式，直到用户明确保存。220 项 Vitest、4 条 Edge E2E（含丢弃后重载不再提示、恢复立即进入编辑器）、19 项 Rust 测试通过，待新安装包验证 Tauri Store 实际落盘。
- [~] 2026-07-19 修复左侧“最近打开”为空：主题/布局/历史自动保存统一延后到持久化 hydration 完成，避免启动默认空历史抢先覆盖磁盘记录；浏览器重载端到端已验证历史保留，待新安装包复验原生 Store。
- [~] 2026-07-19 修复“上次读到这里”详情不准：不再使用“滚动百分比 × Markdown 行数”猜章节，退出时直接记录阅读视口附近的真实渲染正文和最近标题，百分比仅作语义定位失败后的兜底；新增语义锚点回归，待长文安装包实测停留时长和恢复位置。
- [~] 2026-07-19 修复编辑模式保存后自动跳预览：保存成功只更新路径、已保存检查点和 dirty 状态，显式保留当前 read/edit 模式；服务测试和 Edge 端到端均验证保存后编辑器仍可见，待安装包点击复验。
- [~] 2026-07-19 修复 PDF 右侧溢出：计划阶段按 CJK/全角、英文和等宽字符分别估宽，正文、标题、列表、代码、公式、链接和表格均按可打印宽度换行；绘制阶段再按实际嵌入字体宽度限宽。长中文正文与 12 列宽表黄金 PDF 已重新生成并用 Poppler 渲染检查，无右侧裁切；待用户原文安装包导出复验。
- [~] 2026-07-19 完成导出预检闭环：面板增加“导出 Word/PDF”和“取消”，未完成选择或存在阻塞时禁用导出；选择“保留 / 导出副本中省略”后可直接生成，不必再次点击工具栏。新增 Edge 端到端验证选择、按钮启用、下载与面板关闭，待安装包交互复验。
- [~] 2026-07-19 根据实机截图复核：阅读控制栏改为阅读容器零顶部全宽栏，语义彩带移至其后；阅读任务收纳进“阅读工具”面板，不再在右下角半截悬浮。关闭确认后的已确认退出改用原生 destroy，避免再次被关闭拦截。大文档先合并认知航线/账本的重复 Document IR 解析，后续仍需做面板级按需计算与真实内存基线验收。
- [~] 2026-07-19 取消语义彩带自身的纵向滚动条，仅保留正文主滚动条；长文档彩带最多展示 36 个语义标记和 8 个叠加标记，按全文顺序抽样并保留首尾，避免与正文滚动条并排混淆。待安装包视觉验收。
- [~] 2026-07-20 最新 NSIS 已生成于 `src-tauri/target/release/bundle/nsis/墨阅Markdown阅读器_1.0.0_x64-setup.exe`（5,460,137 bytes，SHA-256 `AF4DAB44102B570658923E309CC47FEAAA1E583623D6696E4EE92BF66C4A29A8`，`NotSigned`）；本轮包含用户最终指定的简洁 M 图标及空白“继续阅读”修复，221 项 Vitest 与生产/NSIS 构建通过；同日较早基线为 5 条 Edge E2E、20 项 Rust 测试通过，本轮未重跑。安装器及 EXE 的 32px 图标已直接提取，哈希一致并确认是指定版本；安装脚本包含独立 `moyue-shortcut-v2.ico` 资源，仍以覆盖安装实机复验为最终状态门槛。
- [~] 文件关联启动尚待验证；已实现桌面 Tauri 与浏览器预览的 Markdown 拖拽打开，以及可关闭、本地持久化的首次启动说明；外部文件监听已实现，待原生验证。
- [~] 已建立并通过基础 Vitest 单元测试；文件权限已完成代码级最小化收敛并有 Rust 路径授权单测，仍需在原生安装包中验证动态授权流程。

### P1：成为优秀阅读器

- [x] 搜索、基础字体/段宽/行高控制、侧栏折叠和响应式单栏、三套布局预设以及字距/段距调节均已实现。
- [x] Mermaid、KaTeX、图片灯箱、代码块复制/语言标签/换行切换已实现。
- [~] 表格横向滚动、冻结表头和第一列，以及本地筛选、排序、转置、行列聚焦、TSV/Markdown/CSV 复制、数值统计、数字格式/手工单位后缀和长单元格展开均已实现且不会改写 Markdown；浏览器全屏、保守单位建议、数值列对齐和单元格/行/列复制已接入；原生 `.xlsx` 与视觉验收待继续。
- [ ] 恢复胶囊、语义彩带、阅读专注套件。

### P2：形成不可替代性

- [~] 文档透镜：认知航线的纯规则编译基础已完成，待接入阅读界面与阅读状态。
- [~] 无侵入批注：稳定锚点、localStorage sidecar、版本化 `.mdreader.json` 格式/迁移、审阅 Markdown 导出、阅读态选区高亮批注，以及可复用的备注/下划线/书签工作台已完成；原生文件 sidecar与主阅读页接入待续。
- [~] 摘录托盘：阅读态选区摘录、localStorage 持久化、查看/删除、Markdown 导出、类别筛选、排序、复制和键盘动作已完成；返回原位置与持久化手动排序待主阅读页接入。
  - [~] Live Mirror：文档缩略彩带、搜索/批注/健康诊断/风险/未完成任务叠加与分段增量计算已接入主阅读工具面板；叠加项可跳回渲染原文。当前内容及叠加可显式打开为只读临时原生镜像窗，关闭不影响主窗；增量 Markdown 渲染、原生环境实测仍待完成。
  - [~] 渲染态对比：语义块、列表、表格和代码块的 LCS/主键比较核心及双栏组件已完成，版本加载与主阅读页接入待续。
- [ ] 中文技术文档排版引擎。

### P3：高保真导出

- [~] DocumentIR 和主题 token（DocumentIR 已完成；六类模板的页面、字体、字号、间距、代码/表格颜色已接入浏览器 DOCX/PDF，页眉页脚、封面和 Office/Typst 后端仍待接入）。
- [~] Smart Table Studio（语义与列宽规划器、独立 sidecar、安全重关联及最小交互工作台已完成；拖拽调宽和 DOCX/PDF 后端消费待实现）。
- [~] DOCX OOXML 后端（浏览器 Blob 下载、标题/段落/列表/代码/真实可编辑表格及六类模板的排版 token 已完成；图片、OMML 公式、横向 section、页眉页脚/封面模板和 Office 视觉回归待续）。
- [~] 独立 PDF 后端（浏览器端最小实现已生成真实矢量 PDF 与分页表格；Typst、CJK 字体嵌入及 PDF/A 仍待后续原生后端）。
- [ ] Word 同版 PDF 模式。
- [~] 导出预检、可信度和黄金样本回归（预检 API/独立面板及单测已完成；真实 DOCX/PDF 渲染回归待后端闭环）。

### P4：Windows 原生体验

- [~] 极速文件关联启动已具备启动参数读取，并提供首次启动的拖入/选择文件说明；安装后双击实测待完成。
- [x] 已实现无边框、置顶、隐藏任务栏的单例快速预览窗口，并接入 Esc 关闭、Enter 转完整窗口、左右方向键浏览同目录 Markdown；资源管理器使用可选右键命令触发，安装版已完成原生验收。
- [x] 置顶阅读、当前目录相邻 Markdown 导航及最近/收藏 Jump List 均已实现；Jump List COM 和快速预览已在最终安装版验证。
- [ ] 安装、升级、卸载和文件关联恢复验证。

## 七、产品成功标准

- [ ] 用户无需学习知识库概念，打开文件即可获得完整体验。
- [ ] 阅读功能的增加不显著拖慢冷启动和文件打开速度。
- [ ] 原 Markdown 永远是用户资产，所有增强数据都可导出或删除。
- [ ] 外部编辑器与本应用可以同时使用，不争抢文件所有权。
- [ ] 导出前能看到真实分页风险，导出后不出现静默内容丢失。
- [ ] Word 表格保持可编辑，PDF 表格保持稳定、清晰和可复制。
- [ ] 核心特色能用一句话解释，而不是依赖长功能列表。

## 八、AI 时代原创创新决策

> 完整的外部查重、第一性原理推导、25 个反常识假设、60 个功能概念、12 个组合创新、Killer Feature、三种未来版本和自审结果见：[AI时代Markdown阅读器原创创新设计.md](./AI时代Markdown阅读器原创创新设计.md)。

### 8.1 品类定义

- [ ] 不把“Markdown + AI 聊天框”当产品创新。
- [~] 已提供本地、无需账号的“阅读任务”工作区：目的、时间预算、文档集合、期待结果与实际结果均可持久化；可加入当前文档或全部已打开页签、逐篇标记完成并返回文件。任务星座已将用户明确加入的文档按完成状态呈现为可回跳的本地来源图；跨文档编织与认知航线状态联动待续。
- [~] 已建立本地阅读理解状态账本并接入主阅读区：懂了、存疑、跳过、不同意四种显式状态，关联稳定原文锚点、目的/备注、低置信可关闭提示及同文档变化影响；阅读任务中可本地汇总当前任务与已打开页签，并由读者显式标记“待验证/已形成判断”。汇总覆盖不全会低置信提示，绝不自动标理解、不调用 AI；跨版本持久索引仍待续。
- [ ] AI 生成内容必须与原文使用不同视觉墨水，并且能一键回到来源。
- [ ] AI 不得替用户标记“已经理解”，不得静默修改、删除或隐藏原文。
- [ ] 行为信号只能作为本地、低置信、可关闭的帮助提示，不能成为使用前提。

### 8.2 Killer Feature：认知航线 Cognitive Route

> 用户选择“为什么读”和“有多少时间”，软件不改原文件，却把原文编译成一条带来源的“前置—结论—证据—风险—行动”路线，并跨版本记住用户懂了什么、哪里存疑、跳过了什么、曾做过什么判断。

- [x] 建立稳定块锚点：内容指纹 + 标题路径 + 前后邻域 + 块类型。
- [x] 建立文档语法 IR，首版只识别前置、结论、证据、风险、步骤五类节点。
- [x] 首版支持“快速了解、判断能否执行、按文执行、完整阅读”四种目的。
- [x] 首版支持 5 分钟、15 分钟、完整三个预算档。
- [x] 每条航线包含 5～12 个节点，每个生成标题、解释和结论均有原文来源扣。（阅读视图可选择目的与时间预算，并点击节点回到原文）
- [x] 支持懂了、存疑、跳过、不同意四种显式阅读状态，已在阅读页与选中文本浮层接入本地持久化操作。
- [x] 生成恢复胶囊：保存上次目的、最后显式理解、未解/待验证事项和全文指纹；恢复前明确提示原文变化，不自动合并或改写。
- [~] 完整原文与航线双向同步，用户可随时回到作者原始顺序。（航线已可跳回可定位原文；阅读状态和完整双向状态尚待实现）
- [x] 无 AI 或无网络时仍能用规则生成基础航线，完整阅读能力不受影响。
- [ ] 暂不做眼动、自动判定理解、永久知识图谱、自动联网核验和自主执行命令。

### 8.3 Top 10 开发方向

1. [ ] **稳定块锚点 + 文档语法 IR**：所有阅读记忆、批注、变化和航线的地基。
2. [ ] **认知航线 MVP**：首个真正改变阅读结构的产品灵魂。
3. [ ] **理解账本 + 恢复胶囊**：从记住像素升级为记住理解、疑问和判断。
4. [ ] **主张—证据扣 + 不确定性墨水**：明确原文、改写、推断和外部核验的边界。
5. [ ] **执行跑道**：把 How-to/SOP 编译成前置、步骤、预期、验证和回滚闭环。
6. [ ] **Live Mirror + 变更爆炸半径**：外部编辑或 AI 改文档后，只重读影响旧判断的部分。
7. [ ] **表格即数据集 + 表格叙事镜头**：筛选、冻结、列类型、异常和可回单元格的洞察。
8. [ ] **保真双生导出**：Word/PDF 分页预检、表格布局、自愈和导出后回归验证。
9. [ ] **任务星座 + 多文档编织**：围绕问题组织文档，不再围绕文件夹组织理解。
10. [ ] **版本谱系 + 矛盾/冲突审阅**：建立跨文件、跨版本的项目真相层。

### 8.4 Top 3 本质差异

- [ ] **目的驱动的认知航线**：文档不再只有作者顺序，阅读由本次任务编译。
- [ ] **跨版本生存的理解账本**：软件记住用户理解、疑问、跳过和判断，而不是滚动条。
- [ ] **跨文档变化影响与项目真相层**：知道一次变化会推翻哪些结论、路线和旧判断。

### 8.5 高保真导出的产品承诺

- [ ] 不宣传无法证明的“任何文档 100% 像素完美”。
- [ ] 对外承诺：**不丢内容；表格可编辑；风险先看见；结果经过验证。**
- [ ] AI 只识别列语义、建议布局和解释风险；最终坐标、分页、字体和 OOXML 属性由确定性引擎决定。
- [ ] 导出体系与认知航线共用 Document IR，但独立验收、独立发布，不让 AI 创新掩盖排版工程质量。

### 8.6 最终下注方向

- [ ] 品牌灵魂下注“目的编译 + 来源可追溯 + 理解可持续”的认知航线。
- [ ] 商业付费楔子下注“表格工作台 + 保真双生导出”。
- [ ] 先验证单文件认知航线是否更快形成正确判断、是否漏关键前提、用户是否愿意第二次继续使用；成立后再扩展多文档和个人知识状态。
