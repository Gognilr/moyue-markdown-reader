# Markdown 阅读器核心完善执行计划

> 状态：五阶段完成（2026-07-19）  
> 范围：只包含用户确认的五类工作；不恢复已软弃用的“阅读工具”“批注与摘录”及其扩展实验。

## 执行顺序与状态

| 阶段 | 工作 | 当前状态 | 完成门槛 |
| --- | --- | --- | --- |
| P0-1 | 大文件内存、打开速度、滚动性能 | 完成 | 建立可重复基线；移除已隐藏功能的预计算；重模块按需加载；长文打开、滚动、切换无明显卡顿且有自动化/实测证据 |
| P0-2 | 打开、保存、关闭、文件关联、外部修改稳定性 | 完成 | 自动化通过；新安装包逐项原生实测；权限、未保存保护、覆盖保存和外部冲突行为均符合预期 |
| P1-1 | DOCX/PDF 图片、中文字体、宽表分页、视觉验证 | 完成 | 黄金样本覆盖图片/CJK/宽表；DOCX 与 PDF 结构检查通过；逐页渲染 PNG 人工检查无缺字、裁切、重叠和分页缺陷 |
| P1-2 | 核心操作端到端回归 | 完成 | 覆盖首次启动、打开、拖入、切换、新建、保存、关闭保护、历史记录、外部修改、打印和导出；形成统一可重复命令 |
| P2 | XLSX、Jump List、资源管理器快速预览 | 完成 | XLSX 可导出并由标准表格软件打开；Jump List 显示最近/收藏；快捷预览可从资源管理器触发并通过 Windows 实测 |

## 阶段 P0-1：大文件性能

1. 建立不同规模 Markdown 样本与计时/内存基线。
2. 审计主阅读路径的同步解析、重复 Document IR、健康检查、项目扫描、导出依赖和隐藏实验功能计算。
3. 已弃用能力不得在正常阅读路径中解析正文或注册高成本监听。
4. Mermaid、KaTeX、DOCX、PDF、ZIP、XLSX 等只在文档确实使用或用户触发时加载。
5. 对超长正文采用可取消/分批/空闲调度或虚拟化策略，保证目录跳转与连续滚动稳定。
6. 记录冷启动、打开、首次可读、滚动、切换、峰值内存和关闭后回收结果。

### P0-1 当前证据（2026-07-19）

- 建立 `2.74 MB / 4500 节 / 4501 个标题` 的中文、列表、链接和表格混合样本，可由 `scripts/generate-large-markdown-fixture.mjs` 重复生成。
- 优化前实测：单个新增 WebView 渲染进程私有内存约 `2703.4 MB`，不可接受。
- 已完成：DOCX/PDF 导出引擎按触发加载；软弃用工作台不再预计算 DocumentIR、健康检查、资源清单、批注 DOM 和阅读账本；次要导航分析延迟执行。
- 已完成：超过 `500,000` 字符进入大文件轻量模式，按安全标题/段落边界分页；正文只渲染当前页；目录只保留 180 项窗口并支持全文标题筛选；大文件关闭 Live Mirror 扫描。
- 首轮优化后实测：新增渲染进程私有内存约 `254.1 MB`，相对基线下降约 `90.6%`；主进程窗口就绪约 `338 ms`；两次大跨度滚动往返约 `188 ms / 536 ms`，窗口持续响应。
- 最终 EXE 复测：大文件页内表格改为只读基础表格后，主渲染进程私有内存进一步降至约 `173.9 MB`，相对原始基线下降约 `93.6%`；当前页 `12` 次连续 PageDown 往返约 `261 ms`，滚动后目录同步到第 17 节，窗口持续响应。
- 结论：P0-1 的代码、自动化样本和原生性能门槛均已通过，阶段关闭。

## 阶段 P0-2：Windows 原生稳定性

### P0-2 当前证据（2026-07-19）

- 命令行路径打开和相邻文件切换已通过，未出现无修改误报。
- 应用关闭保护已原生实测：取消后保持运行；放弃并退出后窗口与进程正常结束。
- 原生页签激活已收口到同步会话切换，并补充指针按下激活、`Ctrl+E` 阅读/编辑切换和编辑器自动聚焦；A/B 页签点击后正文、标题、目录一致。
- 同一路径首次保存与第二次覆盖保存均通过，磁盘内容逐次核对成功，保存后脏标记消失。
- 外部修改验收通过：干净文档自动刷新；本地存在未保存修改时出现“采用磁盘版本 / 保留本地修改”冲突条，选择保留本地后磁盘版本未覆盖草稿。
- Edge E2E 已覆盖拖入与文件会话；Rust 原生边界只允许用户授权目录、批准的 Markdown/sidecar 与单个导出目标，越界路径和不支持格式会被拒绝并由 UI 统一显示保存失败。
- 最终 NSIS 已完成覆盖升级、卸载和重装实测：升级/卸载均返回 0；卸载后安装目录、卸载项和快速预览注册表键消失；重装后 `.md/.markdown` 打开命令重新指向已安装 EXE。
- 安装版关联参数打开黄金 Markdown 后，标签和标题命中目标文件，本地图片自然尺寸为 `256 × 256`；命令行打开、选择器、拖入、切换、新建、首次/覆盖保存、关闭保护与外部冲突已有自动化和原生组合证据。
- 结论：P0-2 门槛通过，阶段关闭。

1. 安装后双击 `.md` / `.markdown` 直接打开目标文件。
2. 验证选择器打开、拖入、历史记录重新授权、切换与新建。
3. 验证首次保存、连续覆盖保存、另存为、只读/无权限失败提示。
4. 验证无修改关闭、单/多脏页签保存并退出、放弃退出和取消。
5. 验证外部编辑器写入后的自动刷新、冲突选择、滚动位置保持。
6. 验证安装、升级、卸载及文件关联恢复。

## 阶段 P1-1：DOCX/PDF 质量

### P1-1 验收证据（2026-07-19）

- DOCX/PDF 均已支持同目录受限本地 PNG/JPEG 图片真实嵌入；原生读取只允许当前 Markdown 目录内的相对资源，限制格式和 20 MB 上限，最终 Rust 边界测试随 `cargo test` 19/19 通过。
- PDF 在 Windows 原生环境读取并嵌入 CJK 字体子集；黄金 PDF 使用 SimHei，中文可复制、搜索，未出现问号替换，字体描述符确认包含嵌入字体文件。
- 12 列宽表自动采用横向页面；50 行长表在 DOCX/PDF 中跨页，表头重复，行号 1–50 与末列连续；DOCX 保持真实可编辑表格。
- 黄金样本保存在 `test-artifacts/export-quality/`：DOCX 经 Microsoft Word 转为 14 页 PDF，独立 PDF 为 6 页；两者全部逐页渲染 PNG 并通过联系图和单页原图检查，无缺字、裁切、重叠、空白断页或内容丢失。
- `markdownToIr`、DOCX、PDF 与黄金样本专项测试共 21 项通过；生产构建通过。LibreOffice 在本机未安装，因此本轮 DOCX 视觉基准以 Microsoft Word 为准，不把 LibreOffice 兼容性冒充为已验收。
- 结论：用户指定的图片、中文字体、宽表分页和视觉验证门槛已通过，P1-1 阶段关闭；旧路线图中的 Typst、PDF/A、`reference.docx` 与更大组合矩阵仍是独立增强项。

1. 补齐本地图片嵌入和损坏图片诊断。
2. 为 PDF 嵌入可用 CJK 字体，验证复制/搜索中文。
3. 宽表支持横向页或明确拆分策略；长表重复表头、普通行不拆分、超高行明确续页。
4. 建立中英混排、图片、公式、代码、2/4/8/12 列及 1/50/500 行黄金样本。
5. DOCX 使用标准渲染流程逐页生成 PNG；PDF 使用 Poppler 逐页渲染并检查。

## 阶段 P1-2：端到端回归

### P1-2 验收证据（2026-07-19）

- 新增 Playwright + Microsoft Edge 浏览器端回归 `e2e/core.spec.ts`，2 个完整场景覆盖首次启动卡片、文件选择器、拖入、正文渲染、阅读/编辑切换、新建、多页签未保存保护、保存下载、打印调用及 DOCX/PDF 下载，当前 2/2 通过。
- 新增统一命令 `npm run test:core`，按顺序运行完整 Vitest、Playwright 核心场景及 Rust 原生边界测试；浏览器层和 Tauri 原生层分别报告，不用浏览器模拟冒充 Windows 文件系统验收。
- 最终门禁为 Vitest `75` 个文件 / `217` 项、Playwright/Edge `2/2`、Rust `19/19`，生产构建和 NSIS 打包均通过。
- 端到端编写过程中发现并修复关闭非当前脏页签时错误调用“保存当前文档”的缺陷；现在按待关闭页签 ID 保存，且不会覆盖当前活动文档会话，并补服务回归测试。
- Windows 原生层已独立实测命令行/文件关联打开、页签切换、首次/覆盖保存、关闭保护、自动外部刷新、脏文档冲突选择和安装升级卸载；拖入与导出由 Edge E2E 覆盖，权限边界由 Rust 测试覆盖。
- 结论：核心操作已有可重复自动回归与独立原生证据，P1-2 阶段关闭。

1. 优先覆盖会造成数据丢失或无法打开的核心路径。
2. 浏览器层回归与 Tauri 原生回归分开，不能相互冒充。
3. 测试失败时保留截图、日志、输入样本与复现步骤。
4. 统一验证至少包含前端单元/组件、Rust、生产构建和核心 E2E。

## 阶段 P2：桌面与表格增强

### P2 验收证据（2026-07-19）

- XLSX 采用轻量 OOXML ZIP 生成器，只在点击“导出 XLSX”时加载；导出文件保存在 `test-artifacts/xlsx/table-export-golden.xlsx`。Edge E2E 校验工作表 XML，本机 Microsoft Excel COM 以只读方式打开并读取 `A1=列一`、`B1=列二`、`A2=A`、`B2=B`，使用区域为 `2 × 2`。
- Windows Jump List 使用原生 COM 提交“收藏”和“最近”两类，最多各 10 个，仅接受存在的 Markdown 路径并去重；安装版 IPC 调用成功，系统 `AutomaticDestinations` 文件时间随提交更新。
- 资源管理器快速预览采用可选的 `.md/.markdown` 右键命令，明确提供安装/移除入口；命令精确指向已安装 EXE 的 `--quick-preview "%1"`。轻量窗口不渲染阅读工具、语义条、建议、恢复卡片或表格工作台，`Esc` 关闭；安装版实测正文、中文、宽表和 `256 × 256` 本地图片正常。
- NSIS 卸载钩子实测会清理两种扩展的快速预览键；重装后保持默认关闭，只有用户主动启用才写入注册表。
- 结论：P2 三项代码、自动化与 Windows 原生门槛全部通过，阶段关闭。

### 非五阶段阻塞项

- `npm audit --omit=dev` 当前仍报告现有 Mermaid/KaTeX 依赖链 `1 high / 8 moderate`；`npm audit fix --dry-run` 会升级 Mermaid 解析器并大幅重排依赖树，因此本轮未在功能验收后冒险自动套用。此项保留为独立依赖升级回归，不影响上述五阶段已通过的功能门槛。

1. XLSX 仅导出当前表格或明确选择的表格，不修改原 Markdown。
2. Jump List 从最近记录和收藏生成，失效路径不直接启动。
3. 快速预览提供明确的安装/卸载入口，不注册不可恢复的全局快捷键。
4. 三项均为低优先级；不得拖慢普通打开，也不得扩大默认文件权限。

## 状态规则

- `执行中`：已开始实现，但至少一个门槛未通过。
- `待原生验收`：代码与自动化通过，Windows 实机尚未通过。
- `待视觉验收`：结构通过，但逐页图像检查尚未通过。
- `完成`：对应代码、自动化、原生或视觉门槛全部通过。
